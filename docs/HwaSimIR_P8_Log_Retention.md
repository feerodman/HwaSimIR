# P8 logs 实际清理与保留

实际 HEAD 为 `44ce295d61e5f4e5ccab990260e18964c4e72b11`。开始时只有用户 P8 附件未跟踪，未 reset、clean、回退或提交。

唯一删除根为 `D:\HwaSimIR\logs`，已确认是 D 盘 NTFS 普通目录，无重解析点。只枚举路径、大小、修改时间；读取必要交付清单和部署 SHA 清单，没有遍历读取历史正文或为候选解码视频、重算大文件哈希。没有删除 logs 外文件。

## 已实际执行

| 项目 | 结果 |
|---|---:|
| 清理前文件数 | 314,598 |
| 清理前逻辑大小 | 220,924,214,538 字节，约220.92 GB |
| 实际删除目录项 | 279,275 个文件 |
| 删除逻辑大小 | 207,184,537,413 字节，约207.18 GB |
| 剩余原盘点数据逻辑大小 | 13,739,677,125 字节，约13.74 GB |
| 删除开始前 D 盘可用 | 47,291,174,912 字节 |
| 删除完成后 D 盘可用 | 254,785,658,880 字节 |
| 观测到的实际可用空间增量 | 207,494,483,968 字节，约207.49 GB |
| 变化或无法独占写访问的跳过项 | 204 |
| 跟随链接 / 原地截断文件 | 0 / 0 |

逻辑大小与实际卷可用空间分别记录，不将二者等同；卷可用变化还受文件分配粒度和同期其他程序影响。随后将约98 MB的本轮纯元数据盘点CSV可逆压缩，避免留下另一份庞大明文盘点。

删除主体为多轮部署的重复 `stage/Config`、淘汰试跑、重复源码解包、旧大日志和可重建PFM/RGB/RAW导出。对名称含snapshot的Config副本，先确认其部署清单哈希与最终C4清单相同，且正常Config中对应原文件存在且大小一致；只有这种已被完整原件覆盖的恢复副本才删除。潜在唯一恢复资料和许可证/密钥文件跳过。

每个候选在删除前再次核对绝对路径、全部父目录、重解析属性、大小和修改时间。Windows打开文件时不允许写共享，核对该句柄的最终路径，然后通过同一句柄删除目录项；已打开写句柄的文件不会被强制解锁或截断。保留空目录，不用无边界递归删除。

## 保留原位置

- P7 NVG雨/MWIR雪两组完整十分钟原MP4、未压缩正文和索引、验证结果、输入与有效配置、计时资料。
- `D_C4_nvg_rain60`少182条输入的完整故障包；`C1_board_cloudy_asset60`无降水60Hz基线；MWIR雪60Hz失败基线。
- P7交付页面、全部十段短片、图片、索引/正文及它们引用的完整源案例；两个UI实屏和字段回读资料。
- P7照明检查、基线与源码阶段快照、最终本机ELF、部署收据/清单、回滚身份。Windows `releases` 和板卡快照完全未参与清理。
- `_keep`、`_maintenance`、`.keep`保护目录、P8新证据和未能确认用途的小型资料。

没有移动长测和交付依赖，因此原始复验入口和交付链接继续有效。保留集的精确路径见 `logs/_maintenance/P8_protected_paths.json`。

清单：`P8_cleanup_plan.csv`（分组计划）、`P8_cleanup_result.csv`（实际分组结果）、`P8_deleted_entries.csv.gz`（完整逐项删除/跳过记录）、`P8_inventory.csv.gz`（完整只读元数据盘点）和 `P8_cleanup_summary.json`，均位于 `logs/_maintenance`。gzip文件可直接用Python `gzip.open`读取，不影响任何原始录像或标注复验。

## 本轮证据限制

P8测试关闭重复Annex-B H264转储，只保存接收MP4、正文和逐帧身份/时间索引；默认不启用全帧浮点/RGB连续导出。清理、全量录像验证和大目录哈希不与性能计时同时运行。P8收尾清理结果见下节；正常最终测试和未解决故障的唯一证据均已保留。

## P8收尾暂存清理（独立批次）

最终完整板端/回滚校验通过后，仅清理本轮`logs/p8/deploy_*/stage/Config`中与正式清单同哈希的重复项。删除38026个文件，逻辑28,867,718,012字节；D盘可用由221,223,432,192增至250,121,515,008字节，实际增加28,898,082,816字节。30项无法独占打开而跳过，18项控制/唯一内容保留。各部署清单及版本文件另存`retained_*`，所有录像、正文、逐帧索引和失败包保留。没有跟随链接或截断共享inode。

完整清单位于`logs/_maintenance/P8_stage_cleanup_plan.csv.gz`、`P8_stage_deleted_entries.csv.gz`、`P8_stage_cleanup_summary.json`。这是本轮新暂存的收尾清理，与前述历史207.49GB分别计量；实际卷增量受同时生成的小型验证CSV影响。

普通运行无逐帧PFM/RGB导出；本轮仅六张指定帧PFM用于明确的覆盖诊断，并标明跨试跑毫秒时间差。默认保留一份接收MP4，不再复制整段裸H264。最终交付页只引用保留目录。
