# P13 原始 1.txt 全量输入与大气覆盖审计

## 原文件身份与解析

- 文件：`DataDrivenTestQT/1.txt`
- SHA-256：`f2c3db00d71012cd28336b5077e03ff8d4ed1eda2698fd900486ae9cdfdd7901`
- 大小：2,057,485 bytes；物理行 4319；表头 1 行；有效数据 4318 行；每行 63 列；空行 0。
- 严格解析器完整扫描后才允许发送：表头和空行不是数值错误，非法行会给出源行号并阻止运行。
- 原文件没有被写回、改名或用 `ordinary_demo_1km.txt` 替代。

## 生产代码真实消费映射

| 消费项 | 原文件列（从 1 开始） | 实际范围/语义 |
|---|---:|---|
| 源时间 | 1 `Time(ms)` | 20–43190 ms，固定 10 ms 步长，源跨度 43.170 s |
| 文件距离 | 2 `distanceDAM_RTState(m)` | 2428.921–22274.208 m；仅审计，不作为正式大气夹距输入 |
| 观察者位置 | 3–5 | 纬度 40.000050–40.097901°；经度 99.979570–100.000000°；高度 10998.44238–11970.00586 m |
| 观察者姿态 | 6–8 | yaw 0–360°；pitch -0.003–9.427°；roll -21.305–0° |
| 观察者速度 | 9 | 68.430–277.854 km/h |
| 目标位置 | 11–13 | 纬度 40.101206–40.199973°；经度 99.989793–99.990000°；高度 9733.5–10000.05371 m |
| 目标姿态 | 14–16 | yaw 180–180.163°；pitch -0.263–0°；roll -0.027–0.002° |
| 目标速度 | 17 | 152.761–333.293 km/h |
| 状态字段 | 18、29、30、54–56 | `MissileType/strickenFlag/damageFlag/ViewValid/TrackingState/HitState` 均审计；未新增毁伤模型或私自把状态接到热点 |

生产预检使用两端经纬高重建视线：水平距离 0.943999514–22.246719475 km，斜距 2.427569473–22.269183094 km。文件距离与重建斜距差 0.954885–5.132836 m。

协议身份固定为 `outerPlatID=1001`、`sensorID=2`，目标按完整键 `targetType=34/targetPlatID=3/targetID=3` 解析；没有放宽成仅 targetID 匹配。

## 环境与数据查询范围

原轨迹两波段查询清单共 8636 条（4318×SWIR + 4318×MWIR），生产查询结果为 8636/8636 有效、0 失败；透过率实际范围 0.713294182389–0.970613474789。

| 查询量 | 原始轨迹普通回放实际值 |
|---|---|
| 波段 | SWIR、MWIR，分别全量回放 |
| 大气/气溶胶 | Mid-Latitude Summer / Rural |
| 观察者高度 | 10.99844238–11.97000586 km |
| 目标高度 | 9.7335–10.00005371 km |
| 斜距 | 2.427569473–22.269183094 km |
| 能见度 | 6.0 km |
| 相对湿度 | 76.18%，在实测 RH60/RH85 分量间插值 |
| 仿真 UTC | 2026-09-06 03:30；控制端与渲染端日期身份一致 |
| 太阳天顶角 | 41.426922901–41.508983742° |

天气不是 `1.txt` 的列；晴、云、雨、雪由每个受控普通运行的配置/事件注入并在事件时间表中留证。天气没有通过篡改高度或轨迹实现。

## 原文件实际范围与额外 50 km 要求

必须分开理解：

- 原文件实际范围：观察者 10.998–11.970 km、目标 9.7335–10.0001 km、斜距 2.4276–22.2692 km。原文件不包含 50 km。
- 额外通用覆盖：单独验证高空、不等高、最大 50 km 边界，不冒充原轨迹数据。

正式 P13 增量网格为：SWIR/MWIR；Mid-Latitude Summer/Rural；RH30/RH60/RH85；观察者高度 10.95/12.0 km；目标高度 9.7/10.05 km；斜距 2.4/5/10/20/23/35/50 km；能见度 6 km；太阳天顶角 20/45°。共 336 个五分量完整顶点，来自 720 次受许可 MODTRAN 5.2.1 分量运行。

查找只允许在实测单元内对分量线性插值、对 tau 采用光学厚度插值；禁止外推、夹距、缺失填零和 `tau=1` 回退。网格 tau 为 0.619654906579–0.982341925805，随距离非增，RH 响应检查通过。

## 数据保留与共同身份

- 发布前旧 LUT 1724 行作为字节完全相同前缀保留；旧规范化摘要前后相同。
- NIR 1005 行前后不变，没有改名为 SWIR。
- 正式 LUT 2060 行，SHA-256 `de72d333dfab19989ba56856075a7d95ea5d9305f84bc1ab2a3ef9e5a97464be`。
- 共享覆盖 manifest SHA-256 `be2cf0d2e7f89827b44ba0952c89dceb3c040b13c28881ab92ebb7fef4c752dc`。
- 控制端预检与板端加载均校验相同 LUT/manifest 身份；不符合时拒绝启动。
- MODTRAN 原始许可程序与大体积运行目录不进入分发包；交付处理后 LUT、QC、运行/发布 manifest 和哈希，不伪造输出。

详细逐行查询见 `logs/p13/input_audit_final/input_1_query_manifest.csv`；完整 63 列统计见同目录 `input_1_audit.json`。

