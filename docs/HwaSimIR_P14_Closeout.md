# HwaSimIR P14 工程收口

日期：2026-09-21  
状态：工程项已交付；真实标定保持 `NOT_VERIFIED_CALIBRATION`

## 1. 先给结论

1. 原始 `DataDrivenTestQT/1.txt` 已在普通 DDS 链路正常启动并逐行回放。文件未修改，SHA-256 为 `f2c3db00d71012cd28336b5077e03ff8d4ed1eda2698fd900486ae9cdfdd7901`。SWIR、MWIR 均为 4318/4318 输入、输出和录像帧，800×800、DDS-only、身份 1001/2、`P5MaterialView=0`、正式 M1 实际运行。
2. 开头“隐藏”不是启动失败：原文件数据行 1–659 的 `ViewValid=0`，第 660 行才为 1（源时间 6610 ms）。默认发送端原样遵循该标志；几何有效行仍被逐行接受、计数和发送，没有为凑 60 FPS 跳过。测试端另有明确的“演示强制显示”复选框，默认关闭，不修改文件，也不放宽板端生产过滤。
3. 正式坐标原点不再在 INIT 锁死。INIT 预热只使用临时参考；每一世代由首个身份正确、有限、合法范围的实时平台位置提交正式原点。无包、全零占位、INIT/首包异位、目标晚到、`ViewValid=0`、有效后再无效和恢复均由独立外部 DDS fixture 验证；fixture 不读取 `1.txt`。
4. 黑色尾焰的两个实际根因均已修复：一是正式大气尚未就绪时节点门控为不可见；二是实际 priority=100 的外置 `Config/GameVFX/sprite.frag` 把 SI 辐射度截断到 0–1 并再次叠加经验雾。现路径保留 SI 单位、straight alpha、一次介质覆盖，未使用 `tau=1`、整体加热、强制白色或显示增益。
5. 云、能见度、雨、雪均以独立民用地面卡车 fixture 做两波段混合 DDS 录像；原始高空轨迹未被篡改来制造天气效果。世界云、稳定 cloud ID、对象池、LOD 和 Batch 几何保留。
6. DataDrivenTestQT 的目标类型已经是“名称 + 真实协议码”的 `QComboBox`，INIT 时冻结；Qt `QTest` 对生产窗口实际执行鼠标/键盘点击，选择民用车辆 `0x55`、勾选演示显示、点击 INIT、验证冻结、点击 RESET、验证解锁。没有用命令行或环境变量替代该 UI 操作。

## 2. 原始 1.txt 的真实消费范围

审计入口：`logs/p14/original_input_audit_final/input_1_audit.md` 和 `input_1_audit.json`。生产 LUT 查询复核结果为 `PASS_INPUT_AND_ATMOSPHERE_QUERY`：两波段共 8636 次查询，8636 有效、0 失败、0 顶点失败、0 边界失败；查询 tau 范围 0.71131652222–0.969059247182。

| 项目 | 原文件实际范围/语义 |
| --- | --- |
| 行与表头 | 1 个表头、4318 个数据行、无空行；解析器不再把表头或空行报为业务错误 |
| 源时间 | 20–43190 ms，严格 10 ms 步长，源时间跨度 43.17 s |
| 发送/视频节拍 | 每个已接受业务行一个 DDS 包、60 Hz 行级节拍；标称发送与视频时长 71.95 s，不跳行、不插值、不复用旧帧 |
| 观测平台高度 | 10.99844238–11.97000586 km |
| 目标高度 | 9.7335–10.00005371 km |
| 文件距离 | 2.428921–22.274208 km，仅审计保留 |
| 生产查询斜距 | 由两端经纬高重算，2.427569473–22.269183094 km |
| 观测平台经纬度 | 纬度 40.00005–40.097901°；经度 99.97957–100.0° |
| 目标经纬度 | 纬度 40.101206–40.199973°；经度 99.989793–99.99° |
| 观测姿态 | yaw 0–360°；pitch -0.003–9.427°；roll -21.305–0° |
| 目标姿态 | yaw 180–180.163°；pitch -0.263–0°；roll -0.027–0.002° |
| 观测速度 | 68.43–277.854 km/h |
| 目标速度 | 152.761–333.293 km/h |
| 显示标志 | 数据行 1–659 为 0；第 660 行起首次为 1；位置有效性与显示意图分离 |
| 环境 | Mid-Latitude Summer / Rural；能见度 6 km；RH 85%；UTC 2026-09-06 03:30 |
| 太阳天顶角 | 41.4269229–41.5089837° |

源时间、发送时间、仿真时间和视频 PTS 的关系：`sourceTime` 是文件中不可变的 20–43190 ms；`simulationTime = sourceTime - 20 ms`；发送墙钟按行序约每 16.6667 ms 一包；视频 `sourceSeq` 与接受的业务行一一对应，录制器的 producer PTS 由实际捕获 steady-clock 生成，`frame_index.jsonl` 证明 producer 序列单调且连续。ffprobe 对全部 15 个 MP4 的输入 packet PTS/DTS 审计结果均为 0 重复、0 回退；rawvideo 解码落空设备也 15/15 返回 0。普通 null 输出 mux 会把不等间隔捕获时间量化到自己的输出时基并产生诊断性重复 DTS，因此不再把该 sink 的警告误当成输入 MP4 时间戳失败。原文件 10 ms 源采样间隔不会被伪装成 60 Hz 物理时间。

## 3. 大气支持范围：原轨迹与额外范围分开声明

正式 LUT：2396 个物理数据行，其中 2061 行为生产加载器可用的完整五分量行；P14 以 720 次有许可 MODTRAN 运行新增 336 个完整行，并保持旧数据字节前缀和 1005 个 NIR 行不变。LUT SHA-256 为 `48432459ac56dbb6980f3ca25e2d1e4701763ba3674703051cb0fd45da8c7cd7`；覆盖 manifest SHA-256 为 `f5d27c304b663218b94c9e45bd2bbbb968de1a66695f551ba5c4633f9c4c5d7b`。

原 `1.txt` 的实际查询域就是上一节的 10.998–11.970 km / 9.7335–10.0001 km / 2.4276–22.2692 km，环境只有 6 km 能见度、RH 85%、天顶角约 41.43–41.51°。它不包含 50 km，也不能据此声称所有环境都支持。

额外验证域是离散测量单元，不是任意维度的笛卡尔积：

- 高空不等高域：SWIR/MWIR；观测高度 10.95/12.0 km；目标高度 9.7/10.05 km；斜距 2.4/5/10/20/23/35/50 km；测量能见度 6/23 km，并验证单元内 12 km 插值；RH 30/60/85%；太阳天顶角 20/45°。
- 地面混合天气域：SWIR/MWIR；合法等高对 0.001/0.001 km 和 1/1 km；斜距 0.1/0.5/1/2 km；测量能见度 6/23 km，并验证 12 km 插值；RH 30/60/85%；天顶角 20/45/70°。
- 查询由外部当前有效实时样本的几何和环境驱动。`1.txt` 哈希只作测试血缘，不是外部发送者的业务依赖。
- 超出已测单元时失败关闭；不夹距、不外推、不用零填缺项、不把缺数据改为 `tau=1`。

控制端和板端由同一 LUT/coverage manifest 哈希绑定。真实场景/传感器标定仍为 `NOT_VERIFIED_CALIBRATION`。

## 4. 正常材质、尾焰和合成

本轮正常图全部为 `P5MaterialView=0`，正式 M1 路径实际运行；法线/材质诊断仅作为辅助，不作为正常图替代。民用厢式车/卡车 `0x55` 和通用热源仅承担工程因果验证，不声称任何具体装备的真实红外特征。

尾焰 On−Off 的判据是带符号原始辐射度差，而不是 P13 的绝对差。固定 ROI 的 `On - Off` 平均值为 `+0.067446793258 W/(m² sr µm)`，正样本 728184、负样本 0、最大 `+0.3505859375 W/(m² sr µm)`；原始存储为渲染器 RGBA16F SI 读回并序列化为 RGB PFM。黑热显示只改变观看极性，不改变原始辐射度差的符号。

合成参考单测同时覆盖：热发射 `+1.96`、冷吸收烟 `-0.7`、白热显示 `+0.245`、黑热显示 `-0.245`。实际 draw 的详细单位、alpha、blend、深度和排序见 `HwaSimIR_P14_Effects_Radiance_Contract.md`。

## 5. 最终同版本验证

板端最终 ELF SHA-256：`80267b01054d9fe8e8ef2c3fd8f3e726003794dafc6cc1086958e9713f18841c`，Build ID：`0856b18abf672f4c00904e653ffeb795f5c93e71`。所有下列正式验收使用该 ELF、同一配置身份和同一外置 shader：

| 用例 | 结果 | 板端 accepted→writer 最大值 | 帧率/完整性 |
| --- | --- | --- | --- |
| 原 `1.txt` SWIR 晴天 | PASS，4318/4318 | 冷启动 45.611 ms；稳态 40.826 ms；>80 ms 为 0 | 59.995 fps，MP4 finalized |
| 原 `1.txt` MWIR 晴天 | PASS，4318/4318 | 冷启动 34.441 ms；稳态 39.134 ms；>80 ms 为 0 | 59.998 fps，MP4 finalized |
| MWIR 雪天 300 s | PASS，18000/18000 | 冷启动 69.393 ms；稳态 48.511 ms；>80 ms 为 0 | 59.998 fps，MP4 finalized |
| 生命周期 | PASS | 同一进程 SWIR→MWIR→SWIR、迟到接收端、接收端重启、输入间隙恢复 | 3/3 STOP 排空完成 |

STOP 分项不是一个时间：原 SWIR 控制响应 0.174 ms、渲染排空 49.730 ms、DDS 固定完整排空 5000.120 ms、总计 5049.932 ms；原 MWIR 总计 5049.868 ms；300 s 雪天总计 5055.896 ms。录像完成以接收端队列为 0、输入/写出帧相等、muxer finalized 为准，没有提前杀进程。

板端延时与 Windows Qt paint 分开测量。Qt paint 极端间隔仍有 133.383 ms（原 MWIR）和 316.011 ms（300 s），不冒充板端 80 ms 指标；跨机值是带时钟不确定度的估计，不把两台机器的 steady clock 直接相减。

## 6. 媒体入口

离线交付包根目录的 `index.html` 是统一入口，包含：

- 原 `1.txt` SWIR/MWIR 完整 DDS MP4；
- SWIR/MWIR 的晴天、云/12 km 能见度、雨、雪地面卡车混合 MP4；
- MWIR 300 s 复杂雪天 MP4；
- SWIR/MWIR 首有效位置边界 MP4；
- 尾焰 On/Off MP4、原始 PFM、固定映射和带符号差图；
- received、关键帧、固定映射、AGC/标注、帧身份、事件时间表和媒体 QC。

所有纳入包的 MP4 均实际存在并通过 ffprobe 与完整 rawvideo 解码，15/15 返回 0、800×800 H.264、MP4 finalized。逐 packet 审计进一步确认 PTS/DTS 严格递增。曾观察到的 decode→null-mux non-monotonic DTS 是输出 sink 时基量化的诊断噪声；该过程和结论保留在问题台账，没有删除失败证据或修改业务验收门槛。

## 7. 构建、部署和回滚

- Windows HwaSimIR 与 DataDrivenTestQT 构建已通过；Stage0/3/4/5/7 严格检查和两个直接单测均通过。
- 板端部署同时校验 ELF、运行配置、配置 manifest、实际 sprite/precipitation shader、正式 LUT 和覆盖 manifest；不是只换 ELF。
- 原子回滚演练 `p14rb-20260921-final2` 通过：旧 ELF 启动验证后恢复最终 ELF，再次通过启动、GPU、DDS 和共享大气身份。第一次复制式演练因板端磁盘耗尽失败，trap 恢复最终版本；该失败保留在台账，不计为 PASS。

## 8. 仍未关闭

1. `NOT_VERIFIED_CALIBRATION`：通用材料、民用车辆、热源和环境代理仅为工程验证，不是现场/装备标定。
2. Windows Qt paint 存在上述 >80 ms 长尾；板端最终门限已通过，但 GUI paint 不能合并为同一指标。
3. 高空覆盖是已列离散单元及其单元内插值，不是“3–20 km 高度 × 所有天气 × 0–50 km”的通用全域。

## 9. 保留的历史失败

第一次回滚复制方案因板端磁盘不足失败；恢复 trap 已还原最终版本，随后原子重命名方案通过。该记录不再是未关闭工程项，也从未被当作 PASS，但仍完整保留在问题台账和回滚收据中。
